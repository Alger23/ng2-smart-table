import { Routes } from '@angular/router';

import { DemoColumnIdComponent } from './demo-column-id.component';

export const routes: Routes = [
  {
    path: '',
    component: DemoColumnIdComponent,
  },
];
